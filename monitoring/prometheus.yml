global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: speech-api
    metrics_path: /metrics
    static_configs:
      - targets: ["speech-service:8000"]
